import { SplitBasePipe } from './split-base.pipe';

describe('SplitBasePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitBasePipe();
    expect(pipe).toBeTruthy();
  });
});
